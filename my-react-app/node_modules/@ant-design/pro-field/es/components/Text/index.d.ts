import React from 'react';
import type { ProFieldFC } from '../../index';
/**
 * 最基本的组件，就是个普通的 Input
 *
 * @param
 */
declare const FieldText: ProFieldFC<{
    text: string;
    emptyText?: React.ReactNode;
}>;
export default FieldText;
